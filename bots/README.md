# Bots

Each directory in this group is an independently built and deployed communication service.

All bots implement the network contract in `contracts/BotApi.yaml`. They own their messenger integration, users, dialog state, database and cache.
