# Trackers

Each directory in this group is an independently built and deployed provider-monitoring service.

All trackers implement the network contract in `contracts/TrackerApi.yaml` and advertise their `supportedHosts` during registration.
